import styled from "styled-components";

export const Cart = styled.div`
  width: 60px;
  height: 60px;
  border: 1px solid;
  text-align: center;
  float: left;
  background-color: ${(props) => props.color};
`;
